# Build directories
build/
build-coverage/
build-debug/
build-gcc/
build-release/
cmake-build-*/
out/
ibex.Rcheck/**

# IDE & tool caches
.idea/
.vscode/
.cache/
*.swp
*.swo
*~

# Compile commands (generated)
compile_commands.json

# Generated by r/ibex/configure from src/Makevars.in
r/ibex/src/Makevars

# OS
.DS_Store
Thumbs.db

# Data
.ipynb_checkpoints/**
notebooks/.ipynb_checkpoints/**
ibex_benchmarks.egg-info/**
bench_results/**
benchmarking/data/**
benchmarking/results/compiled/**
benchmarking/results/*_artifacts.tar.gz
*.csv
*.tar.gz
*.tsv
!examples/leetcode/data/*.csv
# Test fixtures: small CSVs that regression tests read (nulls cannot be written as
# an Ibex array literal, so null-bearing test data has to come from a file).
!tests/data/*.csv
*.parquet
*.so
tests/data/iris
Testing/**

# AWS benchmark config (contains bucket name, not secrets but user-specific)
benchmarking/aws/.config

# Python
.venv/
**/__pycache__/**

.claude
.codex

examples/measurements*.txt

# Generated example output
examples/nycflights13/plots*/

# Archived benchmark runs: local provenance for comparing one run against
# another on the same box. The TSVs are already ignored by *.tsv; this also
# covers the manifests that make them comparable.
benchmarking/tpch/results/runs/
