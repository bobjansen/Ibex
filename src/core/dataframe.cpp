#include <ibex/core/dataframe.hpp>

// DataFrame<Schema> is fully header-only (template class).
// This translation unit exists to verify the header compiles.

namespace ibex {

// TODO: Add explicit instantiations for common schema types once
// the DataFrame implementation is fleshed out.

}  // namespace ibex
